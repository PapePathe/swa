dialect:english;

func two()(int, int) { return 2, 3; }

// Test 42: Tuple assignment with array elements
start() int {
  let arr: [2]int;
  (arr[0], arr[1]) = two();
  print("arr[0]=%d, arr[1]=%d", arr[0], arr[1]);
  return 0;
}
