dialect:english;

// Test 22: Function declared to return two but returns nothing – error
func empty() (int, int) {
  // no return
}
start() int {
  let a: int;
  let b: int;
  (a, b) = empty();
  return 0;
}
