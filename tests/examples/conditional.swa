dialect:english;

let salary_string = "100000 EUR";
let salary: Number_32 = 500000;

start() int {
 // salary = 999999;
 // variable new_salary : Number_32 = 1009;

 // if(salary >= new_salary) {
 //   afficher("%s %s %s %s %d", "new salary is greater", "than", "salary", salary_string, salary);
 //   return 10;
 // } else {
 //   print("%s %d", "salary is greater ---- from else block", new_salary);
 // }

  return 100;
}
