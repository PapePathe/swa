dialect:english;

// Test 40: Expression with multiple parentheses
start() int {
  let a: int = ((2 + 3) * (4 - 1)) / (5 - 2);
  // (5 * 3) / 3 = 15 / 3 = 5
  print("((2+3)*(4-1))/(5-2) = %d", a); // Expected: 5
  return 0;
}
