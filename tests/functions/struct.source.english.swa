dialect:english;

struct Person {
  Age:  int,
  Name: string 
}

func age(p: Person) int {
  return p.Age;
}

start() int {
  let p : person= Person{ Age: 40, Name: "Pathe", };
  let myAge : int = 0;

  myAge = age(p);
  print("my age = %d", myAge);

  return 0; 
}
