dialect:english;

// Test 13: Function call with expression arguments
func add(a: int, b: int) int {
  return a + b;
}
start() int {
  let x: int = 2;
  let y: int = 3;
  let z: int = add(x * 2, y + 1); // (4,4) -> 8
  print("add(2*2, 3+1) = %d", z); // Expected: 8
  return 0;
}
