dialect:english;

func variadic printfFn(format: string) int; 
func openFn(name: string, mode: int) int64; 
func closeFn(fd: int64); 
func getpidFn() int;
func getenvFn(name: string) string;

start() int {
  return 0; 
}
