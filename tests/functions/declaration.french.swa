dialecte:français;

fonction variadique printfFn(format: chaine) entier; 
fonction openFn(name: chaine, mode: entier) entier64; 
fonction closeFn(fd: entier64); 
fonction getpidFn() entier;
fonction getenvFn(name: chaine) chaine;

demarrer() entier {
  retourner 0;
}

