dialect:english;

// Test 30: Function that modifies its parameter (call by value, no effect)
func modify(x: int) int {
  x = 100;
  return 0;
}
start() int {
  let a: int = 5;
  modify(a);
  print("a = %d", a);
  return 0;
}
