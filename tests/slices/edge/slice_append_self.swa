dialect:english;
start() int {
  let s: []int = make([]int);
  s << 5;
  
  s << s[0];
 // s[1] = s[1] * 2;
  
  print("count:", count(s), " ");
  print("first-elem:",s[0], " ");
  print("second-elem:",s[1], " ");
  return 0;
}
