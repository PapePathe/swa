dialect:english;

struct Container {
  id: int,
  s: []int,
}

start() int {
  let c: Container = Container{id: 1, s: make([]int)};
  c.s << 10;
  c.s << 20;

  let i: int = 0;
  while (i < count(c.s)) {
    print(c.s[i]);
    i = i + 1;
  }
  return 0;
}
