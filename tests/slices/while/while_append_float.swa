dialect:english;
start() int {
  let s: []float = make([]float);
  let f: float = 0.5;
  let i: int = 0;
  while (i < 3) {
    s << f;
    f = f + 1.0;
    i = i + 1;
  }
  let c: int = 0;
  while (c < count(s)) {
    print(s[c]);
    c = c + 1;
  }
  return 0;
}
