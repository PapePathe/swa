dialect:english;
start() int {
  let s: []string = make([]string);
  let i: int = 0;
  while (i < 3) {
    s << "a";
    i = i + 1;
  }
  let c: int = 0;
  while (c < count(s)) {
    print(s[c]);
    c = c + 1;
  }
  return 0;
}
