dialect:english;

start() int {
  let a: int = 0;
  let b: int = 1;
  
  if (a == 0 || b == 1) {
    print("a == 0 || b == 1 evaluates to true");
  } else {
    print("implementation error");
  }
  return 0;
}
