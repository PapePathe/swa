dialect:english;

start() int {
  let a: int = 1;
  let b: int = 0;
  
  if (a == 1 && b == 0) {
    print("a == 1 && b == 0 evaluates to true");
  } else {
    print("implementation error");
  }
  return 0;
}
