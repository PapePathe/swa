khuien:soussou;

fokhi Node {
  id: konti,
  parent: Node,
}

sodé() konti {
  kouicé node: Node = Node{id: 2, parent: Node{id: 1}};

  // print("id: %d" node.id);
  masen("pid: %d" node.parent.id);

  gbilen 0;
}
