oluasusu:igbo;

nhazi Node {
  id: onuogugu,
  parent: Node,
}

mbido() onuogugu {
  ka node: Node = Node{id: 2, parent: Node{id: 1}};

  // print("id: %d" node.id);
  deputa("pid: %d" node.parent.id);

  weghachi 0;
}
