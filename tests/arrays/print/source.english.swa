dialect:english;

start() int {
  let tableau : []int = []int{1,2,3,4,5};

  print(
    "Array values are: %d %d %d %d %d",
    tableau[0],
    tableau[1],
    tableau[2],
    tableau[3],
    tableau[4],
  );

  return 0;
}
