dialect:english;

struct Point {
  x: int,
  y: int,
}

func modify(p: *Point) int {
  p.x = 100;
  return 0;
}

start() int {
  let p: Point = Point{x: 10, y: 20};
  print("Before: %d", p.x);
  modify(p);
  print("After: %d", p.x);
  return 0;
}
