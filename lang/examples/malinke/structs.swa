// how we declare and structs in Swahili
struct Rectangle {
  width: number,
  height: float,
  name: string,
}

// how we initialize a structs in Swahili 
  const r1: Rectangle = Rectangle {
   width: 10.2,
   height: 45.2 + 6 - 11 * 999,
};

// how we declare and initialize an array
const numbers = []number{1,2,3,4,5,6};
